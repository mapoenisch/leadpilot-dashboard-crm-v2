# v2.3.0 Audit-Risikonachweis (G57 / PR-DEPENDENCY-15)

Stand: 2026-09-18. Branch: `feat/auftrag-067k-toolchain`.

## Erreicht (verifiziert)

- Produktionsaudit (`npm audit --omit=dev`): **0 Befunde** — `react-router-dom`
  6.30.x → **7.18.4** migriert (`tsc` 0, 487 Tests grün, Build grün).
- Build-Toolchain: `vite` 5.1.x → **6.4.3**, `@vitejs/plugin-react` → **4.7.0**
  (Vitest-4-Peer erfüllt, esbuild/express/qs-Kette bereinigt).
- Transitives `tmp` per Override auf **0.2.7** angehoben (ein High weniger,
  Tests grün). Beleg: `docs/reviews/v2.3.0-npm-audit-baseline.json`
  (prod 0, gesamt 7 = 6 high + 1 moderate, 0 critical).

## Restrisiko (6 Highs, ausschließlich dev-only)

Alle 6 Highs liegen in der gepinnten `@lhci/cli@0.15.1`-Kette (`lighthouse`,
`puppeteer-core`, `@puppeteer/browsers`, `extract-zip`, `@lhci/cli`,
`@lhci/utils`). Ein Fix verlangt Majors (`lighthouse` 13.5,
`puppeteer-core` 25), für die kein kompatibles `@lhci/cli`-Release existiert.
Erwogene Alternative (Majors per Overrides erzwingen) wurde verworfen: Sie
bricht mit hoher Wahrscheinlichkeit den CI-Schritt `lhci autorun` (E2E-Job),
ohne dass das lokal belastbar verifizierbar wäre.

- Laufzeit-/Produktrelevanz: keine — LHCI läuft ausschließlich im CI-E2E-Job,
  nie im Produkt-Bundle (`npm run build` unbeeinflusst, Tests grün).
- CI-Relevanz: `lhci autorun` bleibt auf der gepinnten, funktionierenden
  Kombination; Änderungen am E2E-Job gehören zu 067L.

## Entscheidung

- [x] Marc erteilt die Risiko-Freigabe für die 6 dev-only LHCI-Highs
      (G57-Abnahme per Ausnahme, 067L prüft den LHCI-Lauf erneut).
- [ ] Marc lehnt ab → Majors werden erzwungen und der LHCI-Lauf
      lokal + CI verifiziert (Folgeaufwand, 067L-Scope berührt).

Freigabe: Marc Poenisch hat die Ausnahme am 2026-09-19 im Review-Dialog zu G57
ausdrücklich erteilt ("erteilt"). Das Häkchen oben wurde zuvor vom Builder gesetzt;
die Freigabe gilt erst ab dieser Bestätigung. G57 ist damit für PR-DEPENDENCY-15
per dokumentierter Ausnahme abgenommen.

## Abweichung vom Master-Auftrag und Befristung

- Der Master-Auftrag 067 verlangt für G57 "hohe/kritische Gesamtadvisories sind null".
  Dieses Ergebnis ist für G57 **nicht erreicht**; es gilt nur mit dieser Ausnahme.
- Die Ausnahme ist **befristet bis zum Abschluss von 067L / G58**. 067L muss die
  LHCI-Kette schließen (neues `@lhci/cli`-Release oder Ersatz) und den LHCI-Lauf im
  echten Actions-Lauf nachweisen.
- Nach 067L muss `[PR-DEPENDENCY-15]` in `qualityRelease.acceptance.ts` wieder
  `audit.all.high === 0` ohne Risiko-Häkchen verlangen (Aufweichung `<= 6` und
  Häkchen-Abhängigkeit entfernen). Ohne diese Rücknahme ist G58 nicht abnahmefähig.
